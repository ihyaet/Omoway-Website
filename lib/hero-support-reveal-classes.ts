/**
 * Hero supporting line classes — server-safe strings (no `"use client"`).
 * Pair with `HeroSupportingFade` + `data-hero-fade` (fade) or `data-hero-slide` (clip + `y: 100%`, match `HeroHeadline`).
 */
export const HERO_PRE_REVEAL =
  "opacity-0 translate-y-3 motion-reduce:translate-y-0 motion-reduce:opacity-100";
