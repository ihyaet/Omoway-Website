/** Hero background scale + overlay intro — keep in sync with `HeroBackgroundMedia` tween duration. */
export const HERO_BG_INTRO_DURATION_SEC = 1;

/** Scroll lag on hero inner stack — `HeroScrollLayer` (vh of viewport). */
export const HERO_SCROLL_LAG_VH = 22;
export const HERO_SCROLL_SCRUB = 0.45;
